import React from 'react'

function ProjectDescription() {
  return (
    <div>I'll build this project using React as frontend Node and Express for backend and database I'll use MongoDB </div>
  )
}

export default ProjectDescription